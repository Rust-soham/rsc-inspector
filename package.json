{
  "name": "rsc-wrapper-component",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.16.0",
  "scripts": {
    "prepare": "./scripts/prepare-effect.sh",
    "build": "pnpm --filter next-rsc-inspector build && pnpm --filter @rsc-inspector/next-fixture build",
    "check": "pnpm --recursive check",
    "test": "pnpm --recursive test"
  },
  "devDependencies": {
    "@effect/vitest": "4.0.0-beta.107",
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
